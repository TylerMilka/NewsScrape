

  <!-- Page Content -->
  <div class="container">
      <div class="row">
          <div class="col-lg-12">
              <h1>New Scrape  </h1>
                <div class="panel panel-default">
                  {{#each articles}}
                    <div class="panel-body">
                      <a href="{{this.link}}">{{this.title}}</a>
                    </div>
                  {{/each}}
                </div>
          </div>
      </div>
  </div>